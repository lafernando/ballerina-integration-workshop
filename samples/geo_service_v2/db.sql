CREATE DATABASE GEO_DB;
use GEO_DB;

CREATE TABLE GEO_ENTRY (lat DOUBLE, lng DOUBLE, SRC VARCHAR(100), ADDRESS VARCHAR(1000), REF VARCHAR(100));

CREATE TABLE GEO_AUDIT (ts TIMESTAMP, message VARCHAR(2000));
